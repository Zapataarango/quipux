
webdriver{
driver = chrome
autodownload = true
wait.for.timeout = 25000
timeouts.implicitlywait = 25000
}
chrome{
switches = "--incognito;--start-maximized;--remote-allow-origins=*;--ignore-certificate-errors;--disable-extensions;--disable-popup-blocking"
}

serenity {
project.name = quipux
console.headings = normal
logging = VERBOSE
verbose.steps = true
console.colors = true
take.screenshots = AFTER_EACH_STEP

}